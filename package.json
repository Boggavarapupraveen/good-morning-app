{
    "name": "good-morning-app-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "setup": "npm install && cd backend && npm install && cd ../frontend/good-morning-app && npm install",
        "start": "concurrently \"npm run backend\" \"npm run frontend\"",
        "backend": "cd backend && npm run dev",
        "frontend": "cd frontend/good-morning-app && set PORT=3001 && npm start",
        "dev": "npm start"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}